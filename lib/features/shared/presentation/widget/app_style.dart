import 'package:flutter/material.dart';

TextStyle appStyle(double size, Color color, FontWeight fw) {
  return TextStyle(
      fontFamily: 'NotoSans', fontSize: size, color: color, fontWeight: fw);
}
